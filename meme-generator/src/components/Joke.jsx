import React from "react";

export default function Joke(){
  
    // const [messages, setMessages] = React.useState(['d', 'g'])
    // return(
    //     <div>
    //        {
    //         messages.length === 0 ? <h3>You're all caught up!</h3>: <h3>You have {messages.length} unread {messages.length === 1 ? "message" : "messages"} </h3>
    //        }
            
    //     </div>
    // )
    // const[isShown, setIsShown] = React.useState(true)

    // let text = isShown ? 'hide' : 'show'

    // function toggle(){
    //     setIsShown(prev => {
    //         return !prev
    //     })
    // }
    // return (
    //     <div>
    //         <div>{props.joke && <h3>{props.joke}</h3>}</div>
    //         {isShown && <div>{props.punchline}</div>}
    //         <button onClick={toggle}>{text} punchline</button>
    //     </div>
    // )
}